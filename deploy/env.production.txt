# ============================================================
# mafdet.ai 生产环境变量配置
# 复制此文件为 .env 并填入真实值
# ============================================================

# ─── 数据库（Neon PostgreSQL）────────────────────────────────────────────────
# 从 Neon 控制台 → Connection Details → Connection string 复制
# 格式：postgresql://user:password@ep-xxx.ap-southeast-1.aws.neon.tech/neondb?sslmode=require
DATABASE_URL=postgresql://user:password@ep-xxx.ap-southeast-1.aws.neon.tech/neondb?sslmode=require

# ─── AI API（Gemini）─────────────────────────────────────────────────────────
# 从 Google AI Studio (aistudio.google.com) 获取
# 指挥官/汇总者使用 gemini-2.5-pro，执行专家使用 gemini-2.5-flash
GEMINI_API_KEY=AIzaSy-your-gemini-api-key-here

# ─── 认证 ────────────────────────────────────────────────────────────────────
# JWT 签名密钥（随机字符串，建议 64 位以上）
# 生成命令：openssl rand -hex 32
JWT_SECRET=your-random-secret-string-here-at-least-32-chars

# OAuth 配置（可选，仅在启用第三方 OAuth 登录时需要）
VITE_APP_ID=your-oauth-app-id
OAUTH_SERVER_URL=https://oauth.example.com
OWNER_OPEN_ID=your-owner-open-id

# ─── 前端 URL（OAuth 回调后重定向目标）────────────────────────────────────────
# 必须与 OAuth 提供方后台配置的回调域名完全一致
EXPO_WEB_PREVIEW_URL=https://mafdet.ai

# ─── 可选：邮件通知（任务完成后发送报告）──────────────────────────────────────
# 使用 Resend（https://resend.com）发送邮件，免费额度 3000 封/月
# RESEND_API_KEY=re_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# ─── 可选：管理员账号 ─────────────────────────────────────────────────────────
# 用于访问 /admin 后台管理页面
# ADMIN_USERNAME=admin
# ADMIN_PASSWORD=your-admin-password

# ─── 生产环境标识 ─────────────────────────────────────────────────────────────
NODE_ENV=production
PORT=3000

# ─── 测试模式（上线后设为 false 启用次数限制）────────────────────────────────
DEV_DISABLE_LIMITS=false
