# ─── 数据库（Neon PostgreSQL）────────────────────────────────────────────────
# 从 Neon 控制台 → Connection Details → Connection string 复制
DATABASE_URL=postgresql://user:password@ep-xxx.us-east-2.aws.neon.tech/neondb?sslmode=require

# ─── AI API ──────────────────────────────────────────────────────────────────
# API 服务地址（不含 /v1/chat/completions 后缀）
# OpenAI 官方：https://api.openai.com
# DeepSeek：   https://api.deepseek.com
# Groq：       https://api.groq.com/openai
BUILT_IN_FORGE_API_URL=https://api.openai.com

# 对应服务商的 API Key
BUILT_IN_FORGE_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# 使用的模型名称（根据服务商填写）
# OpenAI：   gpt-4o / gpt-4o-mini / gpt-4-turbo
# DeepSeek： deepseek-chat / deepseek-reasoner
# Groq：     llama-3.3-70b-versatile
LLM_MODEL=gpt-4o

# ─── 认证 ────────────────────────────────────────────────────────────────────
# JWT 签名密钥（随机字符串，建议 32 位以上）
# 生成命令：openssl rand -hex 32
JWT_SECRET=your-random-secret-string-here

# OAuth 配置（可选，仅在启用第三方 OAuth 登录时需要）
VITE_APP_ID=your-oauth-app-id
OAUTH_SERVER_URL=https://oauth.example.com
OWNER_OPEN_ID=your-owner-open-id

# ─── 前端 URL（OAuth 回调后重定向目标）────────────────────────────────────────
# 填写你的域名（需与 OAuth 提供方配置的回调域名一致）
EXPO_WEB_PREVIEW_URL=https://yourdomain.com

# ─── 可选：邮件通知（任务完成后发送报告）──────────────────────────────────────
# 使用 Resend（https://resend.com）发送邮件，免费额度 3000 封/月
RESEND_API_KEY=re_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# ─── Node.js 运行环境 ─────────────────────────────────────────────────────────
NODE_ENV=production
PORT=3000
